let maxTokens = 100;
let patientName = 'Patient';
let message = 'Token assigned'


for(let tokenNumber = 1; tokenNumber<=maxTokens; tokenNumber++){
    console.log(`${patientName} ${tokenNumber}`)
    console.log(`Token ${tokenNumber} assigned`);
}